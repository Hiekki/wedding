import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Haley and Kevin | FireGamer3',
    description: 'he almost like a true gamer tho',
};

export default function FAQsPage() {
    return (
        <div className='flex items-center text-center justify-center min-h-[calc(100vh-6rem)] lg:min-h-[calc(100vh-9rem)] w-full font-alex text-7xl'>
            {`o hai 🙃`}
        </div>
    );
}
